<style>
  h1 {
    text-align: center;
  }
</style>

<p style="color: green"><%= notice %></p>

<h1>Productos</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">Precio</th>
      <th scope="col">Descripcion</th>
      <th scope="col">Cantidad</th>
      <th scope="col">Ver detalles</th>
    </tr>
  </thead>
  <tbody>
    <% @products.each do |product| %>
      <tr>
        <th scope="row"> <%= product.id %> </th>
        <td> <%= product.precio %> </td>
        <td> <%= product.descripcion %> </td>
        <td> <%= product.cantidad %> </td>
        <td><%= link_to "Mostrar producto", product, class:"btn btn-outline-dark btn-sm" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<div style="display: flex; justify-content: center; margin-bottom: 20px;">
  <%= link_to "Añadir nuevo producto", new_product_path, class: "btn btn-outline-dark btn-m" %>
</div>
<%= will_paginate @products, renderer: WillPaginate::ActionView::BootstrapLinkRenderer %>

